// Paste the deployed Google Apps Script Web App URLs here once you've set them up
// (see /forms/README.md for the setup steps). Leave as empty strings until then —
// the forms will show a friendly "not connected yet" message instead of failing silently.
window.OFE_CONTACT_SCRIPT_URL = "";
window.OFE_VISION_SCRIPT_URL = "";
